{
  "name": "ai-podcast",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
