{
  "name": "@from-fed-to-chain-mono/mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "flutter test",
    "test:coverage": "flutter test --coverage",
    "lint": "dart analyze .",
    "format": "dart format .",
    "format:check": "dart format --set-exit-if-changed .",
    "dup:check": "pnpm --dir ../.. exec jscpd apps/mobile/lib/ --config .jscpd.json --formats-exts dart:dart"
  }
}
